# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node
node_modules/
.next/
out/
dist/
*.tsbuildinfo

# Env
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db

# Data (regenerate via scrapers). snapshot.example.json IS tracked — it's a
# frozen reference snapshot so `npm run dev` works without running scrapers
# first. The live snapshot.json is generated by CI and not in version control.
# thrustcurve_aerotech.json is tracked too (refreshable but slow-changing).
data/*.db
data/*.db-journal
data/snapshots/
data/snapshot.json

# IDE
.vscode/
.idea/

# Claude Code per-machine settings (commit .claude/settings.json if needed; never local)
.claude/settings.local.json
